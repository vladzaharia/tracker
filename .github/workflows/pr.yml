name: PR Build and Deploy

on:
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    uses: ./.github/workflows/shared-build.yml
    secrets: inherit

  fix:
    needs: ['build']
    uses: ./.github/workflows/shared-fix.yml
    secrets: inherit
    permissions:
      security-events: write

  test:
    needs: ['build']
    uses: ./.github/workflows/shared-test.yml
    secrets: inherit
    permissions: write-all

  deploy:
    needs: ['build', 'fix', 'test']
    uses: ./.github/workflows/shared-deploy.yml
    with:
      environment: staging
      baseUrl: https://staging.track.vlad.gg
    secrets: inherit

  e2e:
    needs: ['deploy']
    uses: ./.github/workflows/shared-e2e.yml
    with:
      environment: staging
    secrets: inherit
    permissions: write-all
